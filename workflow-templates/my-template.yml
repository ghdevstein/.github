name: "GHDevstein CI/CD Template"
description: "Run tests and deploy code to production"
branding:
  icon: "play-circle"
  color: "blue"

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run tests
        run: echo "Running tests..."
